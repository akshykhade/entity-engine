import type { WorkflowDefinition } from "@crud-engine/workflows";

/** Enable when Member has a lifecycle column (e.g. status). */
export const memberWorkflow: WorkflowDefinition | undefined = undefined;
